import React, { useState } from 'react';

const useParts = () => {
    const [parts, setParts] = useState([]);
    

    return (
        <div>
            
        </div>
    );
};

export default useParts;